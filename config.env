#内核源码地址
KERNEL_SOURCE=https://github.com/LineageOS/android_kernel_xiaomi_sm8250
#内核源码分支
KERNEL_SOURCE_BRANCH=lineage-23.2
#内核编译配置
KERNEL_CONFIG=vendor/kona_defconfig
#内核Image.gz-dtb or Image
KERNEL_IMAGE_NAME=Image
ARCH=arm64
#参数
EXTRA_CMDS:LD=ld.lld HOSTCC=gcc
EXTRA_CMDT:LLVM=1
EXTRA_CMDU:LLVM_IAS=1

########################################################################################################################################################
# 编译方案'A'clang+gcc编译 'B'clang编译
METHOD_OK=A

# 构建兼容性修复方案 (none=禁用 sm8250-4.19=SM8250+4.19内核)
BUILD_FIXES=sm8250-4.19

# 编译器选择'true'将使用自定义编译器Clang  'false'将使用AOSP官方Clang
USE_CUSTOM_CLANG=false

# python切换默认python3(ture将切换到python2)
CUT_PYTHON=false

# 编译器AOSP (如需自定义Clang设USE_CUSTOM_CLANG=true)
CLANG_BRANCH=master-kernel-build-2022
CLANG_VERSION=r450784e
CUSTOM_CMDS:CLANG_TRIPLE=aarch64-linux-gnu-

# 自定义Clang (USE_CUSTOM_CLANG=true时生效)
#CUSTOM_CLANG_SOURCE=https://github.com/ravindu644/Android-Kernel-Tutorials/releases/download/toolchains/clang-r353983c.tar.gz
#CUSTOM_CLANG_BRANCH=master


########################################################################################################################################################

# 开启 LXC and DOCKER (见文末 Action 选项区块)
#LXC_DOCKER=true

# 是否打入LXC补丁 (DroidSpaces补丁已包含相同修复)
LXC_PATCH=false

# 关闭CONFIG_ANDROID_PARANOID_NETWORK配置防止docker and lxc出现网络问题
ANDROID_PARANOID_NETWORK_OFF=true

#######################################################################################################################################################

# 开启 KernelSU flags (见文末 Action 选项区块)
#ENABLE_KERNELSU=true
KERNELSU_TAG=v0.9.5
# Superuser 方案选择 (kernelsu=sukisu=SukiSU-Ultra)
SU_TYPE=kernelsu
SUSU_TAG=main
# 自定义KSU管理器签名 (APK size和hash，留空使用默认)
KSU_EXPECTED_SIZE=
KSU_EXPECTED_HASH=
# kprobe不工作时启用KSU源码补丁 (替代kprobe方式)
APPLY_KSU_PATCH=false

# 额外配置 Configuration
DISABLE-LTO=false
DISABLE_CC_WERROR=true
ADD_KPROBES_CONFIG=true
ADD_OVERLAYFS_CONFIG=true


########################################################################################################################################################

# 开启缓存Ccache
ENABLE_CCACHE=true


# DTBO image
NEED_DTBO=false

# 构建前清理无用包释放磁盘空间 (已用maximize-build-space，默认关闭)
REMOVE_UNUSED_PACKAGES=false

# 构建内核直链接Build boot images
BUILD_BOOT_IMG=false
SOURCE_BOOT_IMAGE=

########################################################################################################################################################
#                    Action 运行时可选配置（config.env 优先级高于 Action 界面选项）                                                                      #
#  用法：取消注释（删除行首 #）即启用，注释（添加行首 #）则回退到 Action 界面选项                                                                     #
#  测试时取消注释即可覆盖 Action 界面设置                                                                                                               #
########################################################################################################################################################

# 启用 KernelSU
#ENABLE_KERNELSU=true

# 开启 LXC and DOCKER 支持
#LXC_DOCKER=true

# 开启 KVM 虚拟化
#ENABLE_KVM=true

# 打入 DroidSpaces 补丁
#DROIDSPACES_PATCH=true

# 应用 DroidSpaces 内核配置
#DROIDSPACES_CONFIG=true

# 应用 DroidSpaces 额外配置 (UFW/Fail2Ban/IPSET)
#DROIDSPACES_ADDITIONAL_CONFIG=true

# Superuser 方案 (sukisu=SukiSU-Ultra, kernelsu=KernelSU)
#SU_TYPE=sukisu
